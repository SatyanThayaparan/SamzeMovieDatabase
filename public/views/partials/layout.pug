div.col-10.ml-sm-auto.px-4(style='margin-top: 40px;')
    if successes
      for success in successes
        div.alert.alert-success(style='text-align: center; width: 50%;') #{ success }
    if errors
      for error, i in errors
        div.alert.alert-danger(style='text-align: center; width: 50%;') #{ error }